kind = "web"
previewPath = "/"
title = "Tesla Homepage Clone"
version = "1.0.0"
id = "artifacts/tesla-clone"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21642

[services.development]
run = "pnpm --filter @workspace/tesla-clone run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/tesla-clone", "run", "build" ]
publicDir = "artifacts/tesla-clone/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21642"
BASE_PATH = "/"
